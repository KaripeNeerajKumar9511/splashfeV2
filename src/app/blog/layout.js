export const metadata = {
  title: "Blog | Splash AI Studio",
  description: "Latest news, trends, and insights on AI, fashion photography, and digital retail from Splash AI Studio.",
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
    },
  },
};

export default function BlogLayout({ children }) {
  return children;
}
